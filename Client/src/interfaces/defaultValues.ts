export interface Errors {
    bandName: string
    bandDiscs: string
    bandGenres: string
    startDate: string
    numbOfMembers: string
}

export interface formManipulation {
    manipulate: boolean
    formName: string
    bandId: string
}